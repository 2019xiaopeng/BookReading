# 安卓本地阅读器（MD/EPUB/TXT/PDF 漫画拆分）PRD

## 1. 背景与机会
- 读书重度用户常同时阅读：epub 正文、md 笔记、txt 小说，以及 pdf 漫画/扫描版。
- 大多数阅读器的优势来自“阅读页体验”（排版、进度、标注、搜索、查找与跳转），而不是云端能力。
- 本项目定位为纯本地：不做账号体系/云同步/跨设备安全策略；仅提供局域网传书作为导入增强。

## 2. 产品目标
- 目标 1：覆盖主流本地格式（md/epub/txt/常见文本/pdf），做到打开稳定、阅读顺滑、进度可靠。
- 目标 2：对 PDF 漫画提供“纵向切段 + 子页序列”阅读体验，适配手机小屏，且不导出大量图片文件。
- 目标 3：提供阅读页必备能力：进度跳转、书签、标注笔记汇总、全文搜索（本地索引）。

## 3. 非目标（明确不做）
- 不做账号/登录、云同步、跨设备进度/标注同步。
- 不做 DRM 电子书破解与付费内容导入能力（只支持用户可访问的本地文件）。
- 不做社区/书评/排行榜等“内容平台”能力。

## 4. 目标用户与场景
### 4.1 目标用户
- A 类：读小说/资料的用户（epub/txt/md），关注排版与标注。
- B 类：读漫画/扫描版的用户（pdf），关注缩放手势、拆分后翻页效率。

### 4.2 核心场景
- S1：从手机本地导入一本书，打开阅读并自动记忆进度。
- S2：阅读中快速调整字号/行距/主题/亮度。
- S3：阅读中添加书签、划线并写笔记；回到汇总页复盘。
- S4：对全书进行关键词搜索并跳转到命中位置。
- S5：在同一 Wi‑Fi 下，从电脑把书传到手机并自动入库；PDF 自动触发后台拆分。

## 5. 产品范围（MVP）
### 5.1 书架与导入
- 书架列表：展示封面（可为生成封面）、标题、作者、格式、进度。
- 过滤：按格式筛选（至少 EPUB/PDF/其他）。
- 导入方式
  - 手机端：系统文件选择器（SAF）选择文件导入。
  - 局域网：传书入口，电脑端访问手机提供的地址进行上传（或手机扫码连接）。
- 入库规则：按文件生成 Book 记录；支持重复导入检测（可用文件名+大小+mtime 或 hash 后置）。

### 5.2 阅读页（通用）
- 翻页方式：点击翻页 / 上下滚动（至少两种）。
- 排版设置：字体、字号、行距、页边距、主题（日/夜/暖）。
- 亮度：阅读页内调节亮度（真实实现调用系统亮度或覆盖层策略）。
- 进度：显示百分比与当前章/页；提供进度跳转（滑条/输入后置）。
- 书签：一键添加/移除；书签列表与跳转。
- 标注与笔记
  - 高亮：多色；保存引用文本（txt/md/epub）或保存坐标（pdf）。
  - 笔记：高亮附带笔记；标注汇总列表；删除。
- 搜索：全文搜索（txt/md/epub）；结果列表按命中展示并支持跳转。

### 5.3 EPUB 引擎（MVP）
- 解析：读取 spine 与 toc（NCX/HTML nav）并生成章节列表。
- 渲染：章节级加载；文本重排版；基础图片展示（复杂 CSS 兼容后置）。
- 定位：章节级 locator（后续升级到 CFI）。

### 5.4 TXT/MD 引擎（MVP）
- 编码：UTF-8 优先，兼容常见编码（GBK/GB18030）识别（后置可更精确）。
- MD：识别标题层级用于目录（至少 H1-H3），代码块等以块形式渲染（简化版）。

### 5.5 PDF 漫画（纵向切段 + 子页序列）
- 阅读：缩放/拖拽（基础）；“子页序列翻页”优先。
- 后台预处理（导入后自动触发）
  - 去白边：计算内容区域 contentRect。
  - 切段：在 contentRect 内做横向投影，寻找空白带作为切线；生成多个 segment rect。
  - 结果存储：只保存每页的 segment 坐标（归一化 RectN），不导出图片文件。
- 阅读期渲染
  - 按 segment 坐标裁切渲染；
  - 位图缓存：内存 LRU（当前+前后 N 个子页）；磁盘缓存放 app cache（可被系统清理）。
- 异常回退：某页切段失败时以“整页单段”展示。
- 参数（MVP 可提供）：切段强度、最小段高、段间重叠；支持“重算本书”。

## 6. 交互与信息架构
### 6.1 页面结构
- 书架
  - 导入
  - 局域网传书
- 阅读页
  - 顶部：返回、书签、菜单
  - 菜单：目录（EPUB）、搜索（非 PDF）、进度跳转、书签列表、标注列表、添加标注、设置
- 列表页
  - 搜索结果
  - 书签列表
  - 标注与笔记列表

### 6.2 关键交互规则
- “返回”优先关闭抽屉/弹层，其次返回上级页面。
- PDF：预处理未完成时进入预处理页；完成后自动切到子页阅读。
- 搜索/书签/标注页打开时记录 returnTo，返回时恢复到原阅读页。

## 7. 数据模型（本地）
### 7.1 Book
- id
- title / author（可从 epub metadata 获取；纯文本可从文件名推断）
- format（MD/EPUB/TXT/PDF）
- sourceUri（SAF Uri）
- addedAt / lastOpenedAt
- progress（0..1）与 locator（json）

### 7.2 Bookmark
- id / bookId
- locator（json）
- label（用于列表展示）
- createdAt

### 7.3 Annotation（Highlight/Note）
- id / bookId
- locator（json）
- color
- text（非 PDF）或 rect 坐标引用（PDF）
- note
- createdAt

### 7.4 PdfSplit
- bookId
- status（pending/running/done/failed）
- params（strength/minSegHeight/overlapPct + algoVersion）
- pageSegments：按 pageIndex 存 `List<RectN>`（RectN 为归一化坐标）

## 8. 技术与质量指标
### 8.1 性能
- 打开速度：10MB txt / 常见 epub 在可接受时间内进入阅读（目标：秒级）。
- PDF 漫画：预处理后台执行；阅读翻页连续性优先，缓存命中时无明显卡顿。

### 8.2 兼容性
- Android 最低版本建议：API 21+（便于使用系统 PdfRenderer）。
- 输入来源：SAF Uri；对权限丢失提供友好提示（重新授权）。

### 8.3 稳定性
- 预处理任务可暂停/重试；失败可回退整页阅读。
- 本地索引可重建；删除书籍时清理对应缓存与索引。

## 9. 风险与对策
- EPUB 样式多样：MVP 只保证基础排版；复杂 CSS 走降级策略或后续引入 WebView 渲染作为兜底。
- PDF 漫画页面复杂：切段可能过切/漏切；通过最小段高、重叠、强度参数 + 失败回退降低损伤。
- 本地搜索：大文件索引耗时；采用增量索引、后台构建、按书缓存索引结果。

## 10. 里程碑（建议）
- M1：书架 + 导入（含局域网传书占位）+ txt/md/epub 基础阅读 + 进度记忆
- M2：书签 + 标注笔记 + 搜索（txt/epub/md）
- M3：PDF 漫画预处理（去白边 + 纵向切段）+ 子页阅读 + 参数重算

